"""
List of data types
"""

"""
Category-1: IMMUTABLE Types
        After creating variable with some values then that values CAN'T be changed
        throught the program. Only we can read/access existing value.

    1. Numbers:
        - There is already a option to store numbers like 10, 12.5

    2. Strings:
        - There is already a option to store name, email-id, address etc
        - Automatically index number will be assigned to each character

    3. Tuple:
        - There is already a option to store MULTIPLE values like list of employee names
        - We can store DUPLICATE values
        - Automatically index number will be assigned to each value

    4. Frozenset:
        - There is already a option to store MULTIPLE values like list of employee names
        - We can store UNIQUE values
        - No, Index number to each value

Category-2: MUTABLE Types
    After creating variable with some values then that values CAN be changed
            throught the program. We can ADD/UPDATE/REMOVE
    1. List:
        - There is already a option to store MULTIPLE values like list of employee names
        - We can store DUPLICATE values
        - Automatically index number will be assigned to each value

    2. Set:
        - There is already a option to store MULTIPLE values like list of employee names
        - We can store UNIQUE values
        - No, Index number to each value

    3. Dictionary:
        - There is already a option to store MULTIPLE values like list of employee names
        - We can store DUPLICATE values
        - MANUALLY, we need to provide index to each value. Called KEY/VALUE pair
"""